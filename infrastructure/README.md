# Infrastructure Configuration

This directory contains deployment and operational templates:
- `docker/`: Dockerfiles and multi-stage container build definitions.
- `nginx/`: Reverse proxy configuration for frontend routing and API gateway.
- `monitoring/`: Prometheus metric scraping configs and Grafana dashboard definitions.
- `deployment/`: Production manifests and environment setup scripts.
